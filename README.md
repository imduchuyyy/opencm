1. Only answer when user mentions
2. Store all messages in Database
3. Use latest 10 messages to for context
4. Allow agent to setup its own configuration (whenever admin wants to change the configuration, it can do so by sending a message with the new configuration)
5. General context (group name, group description, admin name...)
